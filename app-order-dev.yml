server:
  port: 9091
  servlet:
    context-path: /
    session:
      timeout: 300

logging:
  config: classpath:config/logback.xml

spring:
  application:
    name: app-order
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    metadata-map:
      key01: key01
      value01: '{cipher}d4c64600599a18ce254d4ff2e3494f74df7abec4d2c723963691b29ec588a91d'

feign:
  hystrix:
    enabled: true